{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build test",
            "command": "make",
            "args": [
                "test"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}